apply plugin: 'distribution'

tasks.named('jar') {
    archiveBaseName = 'octopus-teamcity-opentelemetry-server'
    manifest {
        attributes(
                'Specification-Title': archiveBaseName,
                'Specification-Version': project.version,
                'Implementation-Title': archiveBaseName,
                'Implementation-Version': project.version
        )
    }
}

dependencies {
    implementation project(':common')
    implementation platform("io.opentelemetry:opentelemetry-bom:1.26.0")
    implementation('io.opentelemetry:opentelemetry-api:1.25.0')
    implementation('io.opentelemetry:opentelemetry-sdk:1.26.0')
    implementation('io.opentelemetry:opentelemetry-exporter-otlp:1.26.0')
    implementation('io.opentelemetry:opentelemetry-semconv:1.27.0-alpha-SNAPSHOT')
    implementation("io.opentelemetry:opentelemetry-exporter-zipkin")
    implementation 'io.grpc:grpc-netty-shaded:1.54.1'

    testImplementation 'org.jetbrains.teamcity:tests-support:2023.03'
    testImplementation 'org.jetbrains.teamcity:server-api'
    testImplementation 'org.apache.commons:commons-lang3:3.12.0'
    testImplementation 'org.mockito:mockito-junit-jupiter:5.3.1'
    testImplementation 'org.mockito:mockito-core:5.3.1'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.9.3'

    provided 'org.jetbrains.teamcity:server-api'
}

test {
    useJUnitPlatform()
}

distributions {
    all {
        distributionBaseName = "server"
        contents {
            from configurations.runtimeClasspath
            from jar
        }
    }
}